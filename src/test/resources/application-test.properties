spring.application.name=Pulse Clinic API Test

# Test Database Configuration - Use CI/Docker PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5433/pulse-db
spring.datasource.username=user
spring.datasource.password=135792468
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.detect-encoding=true
spring.flyway.clean-disabled=false

# JWT Configuration
jwt.secret=b0d86673d17c9b9c3203a7dba9b8d5696423d3480aed6408088fb45907318ccc
jwt.expiration=86400000

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Mail Configuration (test mode - no real emails)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=test@example.com
spring.mail.password=testpassword
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# OTP Configuration
app.otp.ttl-minutes=10
app.otp.request-cooldown-seconds=60
app.otp.max-attempts=5
app.otp.pepper=test-pepper

# Supabase Configuration (test mode)
supabase.url=https://test.supabase.co
supabase.key=test-key
supabase.bucket.avatars=avatars

# File upload limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Logging
logging.level.com.pulseclinic=INFO
logging.level.org.hibernate.SQL=DEBUG
